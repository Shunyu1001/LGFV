# Scope clarification

The user's review clarification during execution requires explicit conditioning
on the linked reference outcome being valid for the corresponding screen
issuer and event. Exact issuer-name matching does not prove same-event pairing,
event-time alignment, or independence. The builder retains available event and
source anchors, reports that alignment is not established, and makes this
condition explicit in every bound row and the generated table and notes.
Without that condition, the screen outcome bounds remain zero to one.

The 158-unit population is a documentary screening universe. Current scope
exclusions are not gold outcome negatives. The putative 191-identity union is
reported as arithmetic only, without a city-platform distribution or bounds.
No scientific criterion, outcome definition, reference label, or denominator is
changed in response to the numerical result.

## First execution failure

Attempt 1 failed before writing result outputs because the source-link guard
required legacy document IDs for every candidate origin. Two inherited
alternative Guiyang origin rows have empty legacy document-ID fields:
`mv_dd84e076bf32` / `pilot_gz_001_alt_rail` (eligible) and
`mv_32559fdf4bd7` / `pilot_gz_001_alt_chengfa` (ineligible). The current crosswalk
instead cites `web_guiyang_2022_midyear_bond_report` and
`web_chinamoney_guizhou_expressway_2024_base`, respectively, through the current
validation source manifest. This is an inventory-location mismatch, not a
missing outcome that may be filled. The repair checks every current scope-source
link, pins that manifest to the same base commit, and explicitly counts the two
empty legacy origin fields. Neither row is dropped. No raw-source access or
new source verification is claimed.
