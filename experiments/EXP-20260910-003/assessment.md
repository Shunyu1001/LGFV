# Human confirmation status propagation

Decision: keep.

The author reports that all 94 current reference labels were human-checked
without revision. The report and a deterministic case-level register now
record that fact. The original AI-produced snapshot is retained byte for byte;
its old pending status is historical metadata, not the current confirmation
status. Current manuscript passages, generated notes, and derived screening
metadata use the confirmation register.

The register covers only the 94 reference cases. It does not confirm the 203
surrogate disclosures, extend review to controls or mechanism indicators, or
populate the separate blinded-coding forms. The reporting date is 10 September
2026; the actual review date and reviewer identity were not supplied.

## Verification

- First execution: all 16 registered commands passed.
- Second execution: all 18 commands passed, including 7 confirmation tests,
  5 role-builder tests, and 27 probability-frame tests.
- The numeric-invariance check passed. Reference outcomes remain 2 substantive
  exits, 82 nominal exits, and 10 functional transfers. The 84-case historical
  subset and 78-case full-controls subset are unchanged.
- The expanded surrogate snapshot and reference snapshot retain their original
  hashes. Only derived review metadata and explanatory notes changed.
- Current validation-frame checks pass for 67 eligible candidates and 488
  citation slots from 159 sources. No frame decision, freeze, or draw changed.
- Local LaTeX compilation produced 79 pages without undefined citations or
  references. Visual checks covered the abstract, confirmation paragraph, and
  updated measurement-file table. The latter's long row was shortened without
  changing its meaning, removing a 25-point overflow.

Three exact authorized provenance-document hashes are recognized by the frame
validator. Tests reject further edits and still protect the original outcomes
and scientific design. This compatibility update does not relax coding or
sampling rules.

## Limits retained

This is author-reported human checking, not documented blinded double coding.
No accuracy rate, kappa, probability-validation estimate, or completed DSL
inference follows from this update. Sparse outcomes and selected overlap
remain substantive limitations. The existing source-extraction warnings and
three small appendix overflows remain recorded in the execution logs. Existing
longtable glue warnings also remain. Older manuscript frame-progress passages
require their separately scoped claim update.

## Execution record

The brief and author report were committed as 8efb821 before implementation.
Two initial patch-context mismatches caused no partial edits and were corrected
after fresh reads. Both subsequent verification runs passed. GitHub fetch
initially timed out after approximately 75 seconds, without remote changes.
Remote publication and Overleaf build verification are recorded separately
after they occur; local success alone is not a synchronization claim.
