# Renewal of one changed public source

Registered before renewal execution on 2026-09-10.
Base: `cfee3b9`, branch `codex/validation-frame-freeze-execution`.
Loop: measurement_validation. Budget: 45 minutes, one implementation and two
mechanical retries. No outcome, scope rule, sampling design, or estimand change.

## Known problem and hypothesis

Experiment 001 reproduced the authorized integration but could not recover an
old Shenzhen SASAC webpage byte for byte. The current page was already opened
and read on 2026-09-10. Its municipal-platform profile, address, and local public
role remain visible; the inherited owner excerpt instead quotes navigation.
These observations are known before registration, not new prospective results.

The falsifiable bottleneck is whether a separately dated retrieval and direct
focal-issuer excerpts can restore current source traceability without changing
any scientific decision or falsely claiming recovery of the historical page.

## Fixed criteria

1. Preserve all 273 existing source-manifest rows and their hashes exactly.
   Append one new source identity ending `_20260910`, with its own retrieval
   date, URL, complete raw/text hashes, byte count, and publication date.
2. Change only the three webpage evidence locators and excerpts for Shenzhen
   Special Zone Construction and Development Group. Its issuer identity,
   geography, public ownership coding, platform role, and eligibility stay
   identical. Other crosswalk rows remain exactly as in the base. Direct
   issuer/public-control, platform-role, and issuer-address evidence must be
   present in continuous text, not website navigation or a third-party address.
   Contradictory current content requires quarantine, not a new decision.
3. The current authority is the Shenzhen SASAC website, publication dated
   2026-08-03. The evidence renews frame provenance only; it does not establish
   a post-exit outcome, ownership percentage, or timing of functional change.
4. The 67 candidates, all 157 origin rows, 24 strata, probabilities, and flow
   table remain byte-identical to the base. All 94 working references, LLM
   surrogates, protected inputs, prior evidence packets, and earlier results
   remain byte-identical. No draw or human outcome confirmation occurs.
5. Reverify all 159 currently cited sources and all 488 evidence slots under
   their own recorded hashes and page bounds, including raw reconstruction of
   the Guiyang text. Run the same full tests and integrity commands as
   Experiment 001. A source-version mismatch must still fail validation.
6. Clean reconstruction from `9977dd7`, followed by the approved integration
   and this explicit renewal, and an idempotent rerun must yield identical
   current outputs. Independent reviewer checks include negative mutation
   tests for old manifests, new metadata, evidence locators, and decisions.
7. Record the old/new source lineage and the fact that the old raw snapshot is
   unavailable. Archive hash-verified current caches locally under a new
   ignored raw-data directory without overwriting any file. Do not distribute
   raw material to GitHub or Overleaf.

## Scope and commands

Allowed: this experiment directory; probability-frame builder/validator/tests;
the current crosswalk and source manifest; current metric outputs; append-only
research ledgers and implementation notes; local ignored source cache archive.
No prose, immutable files, outcome files, or earlier experiment artifacts.

Commands: experiment renewal script, explicit integrated frame rebuild,
current probability-frame validation, historical freeze validation, both
targeted unittest suites, immutable/ledger/label/master-pool validation, and
experiment-local deterministic reconstruction and source-archive audit.
Current-source renewal is an evidence maintenance operation, not relaxation of
the failed exact-historical-recovery criterion in Experiment 001.
