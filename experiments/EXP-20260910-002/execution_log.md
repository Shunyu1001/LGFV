# Execution record

The brief was committed as `4c0d5de` before renewal execution. The current
source had already been opened; its known raw and text hashes and the inherited
navigation-only ownership citation were disclosed in the brief.

1. `prepare_source.py` verified the newly retrieved raw page and complete
   extracted text, then registered the new manifest row, continuous focal
   excerpts, exact field changes, and old/new source lineage.
2. The explicit `--integrate-exp004 --renew-source` rebuild completed with
   67 candidates, 24 strata, and no draw. The current validator returned
   488 verified citation slots. These initial checks were followed by the
   recorded full audit rather than treated as final acceptance alone.
3. `run_audit.py` attempt 1 passed the clean reconstruction, rerun, all
   protected-result checks, 26 probability-frame tests, six historical-package
   tests, and four repository integrity checks. It archived 313 hash-verified
   files without overwriting raw material.
4. A dedicated renewal test was added to require direct focal evidence and
   preservation of every non-evidence field. The manifest mutation test was
   extended to cover both added source versions as well as original rows.
5. Attempt 2 reran all commands with 27 probability-frame tests and six
   historical-package tests. The unchanged local archive was reverified.

Independent AI review confirmed the direct owner/role/address linkage and
unchanged scope decisions, and reproduced the full source validation. No
parallel reviewer wrote project files or supplied human labels.

Browser operations were separately attempted. The in-app browser listed the
Overleaf tabs, but attaching to either tab repeatedly timed out. The native
Computer route explicitly disallowed controlling the Codex app, and that route
was stopped. No Overleaf pull, edit, or compile is claimed from those attempts.
