# Collection package assessment

Status: keep, for the collection instrument and source/identity audit only.
Realized validation inputs remain blocked. No freeze, draw, human coding,
adjudication, label promotion, or analytic authorization occurred.

## Coverage and identity

- 67 current platform-scope-eligible issuer units; 74 originating rows.
- 689 source-locator records: 610 document records and 79 source-page records.
- Every unit has public locators and at least one hash-verified local manifest
  source. All exact origin-document IDs and unit-linked manifest documents are
  retained, including the empty original Guiyang document-list gap.
- All 6,298 current-unit/reference-case pairs were audited. There are no exact
  issuer matches and no exact issuer/event mappings to HC-20260910-001. All 67
  current labels remain missing. The 33 historical-capacity links are not label
  matches. Literal issuer equality and exact source-case/primary-event-document
  identity are required; no aliases or city-label joins were introduced.
- The historical packet contained four units. Three remain, 64 current units
  are additions, and mv_83fa1cb2dc9e is historical-only. The current origin table
  marks that issuer ineligible. Historical files are unchanged.

## Local sources

Of 138 current-unit source-manifest records, 79 raw files verify at the manifest
archive location. Another 56 verify at their document-inventory locations,
giving 135 of 138 verified somewhere among the recorded locators. There are no
hash mismatches. The 59 missing manifest-archive locations are not 59 wholly
unavailable source documents. The three without a verified raw copy at either
recorded location are web_ndrc_jian_jinluling,
web_shenzhen_sasac_special_zone_development (historical), and
web_saac_guiyang_public_transport_current_name. Their public locators and missing
states remain recorded. The renewed Shenzhen source does verify. No source
search or download was performed and no source bytes were changed.

The broader origin-document union includes files present without a prior
manifest hash and files missing locally. The per-record audit distinguishes
those states; inventory metadata or public URLs alone are not recovered raw
documents and source availability is not human-assessed evidence sufficiency.

## Human and design boundaries

Both coder sheets and the adjudication sheet contain exactly 67 stable IDs;
all decision, evidence-assessment, reviewer, date, score, and signature fields
are blank. Platform-scope eligibility is separate from exit-case eligibility.
The 10 no-direct-formal-event screens remain coordinator metadata. Missing
formal/post-event evidence leaves exit eligibility unresolved or ineligible,
the label missing, and institutional change missing. The schema has exactly
four exit labels, with no fifth unresolved outcome. Liquidation remains outside
the declared binary contrast.

All 24 strata and pi=1 values remain an unchanged proposal. Realized
probabilities, actual decisions, outcome counts, agreement, and process metadata
remain null. The coordinator must manage actual authorization, received human
forms, and analytic requests as separate layers. The baseline research-state
ledger is historical context only; later coordinator ledger/README updates do
not invalidate the builder. Scientific input hashes remain strict.

## Commands and attempts

The exact commands and complete stdout/stderr are in attempt_1.json through
attempt_3.json. Attempt 1 passed generation, byte-identical checking, and all
repository validators; one test incorrectly assumed historical membership was
a subset. The first mechanical retry corrected that fixture; all 19 tests and
commands passed. The final coordinator-requested metadata-input correction
added a regression test; attempt 3 passed all 20 tests and every command.
Both retries are documented, and the original generated metadata indexes are
preserved. No further experiment loop or retries were run.

Passing checks: builder, --check, unittest collection suite, human confirmation
register --check, immutable validator, ledger validator, label validator,
master-case-pool validator, and git diff --check. The label validator's warnings
about historical extracted texts absent from this isolated worktree are retained
in the logs. No manuscript/model outputs changed, so no model or paper build was
run. Human-entry bytes did not change across the metadata revision.

Only authorized new paths are delivered. Local attempts.tsv is for coordinator
ledger integration; this worker did not edit global ledgers or merge/push.
