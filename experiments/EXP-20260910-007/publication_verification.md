# Publication verification

Verified on 10 September 2026 by the coordinator.

The accepted paper and implementation commit is
`0734ba985188f19ca258ff941b00978feb0fd0b8`. The coordinator fast-forwarded the
clean main checkout from `4330c57` and pushed both `main` and the isolated
integration branch to `Shunyu1001/LGFV`. The refreshed in-app GitHub page
displayed the new collection section and the default `not_estimated` status.

In the authenticated in-app Overleaf project
`6a36a191808381e139302e51`, the GitHub Sync panel listed the ten new commits
through `0734ba9`. The coordinator clicked Pull GitHub changes into Overleaf.
The subsequent panel reported: No new commits in GitHub since last merge.
The coordinator then clicked Recompile.

The resulting PDF download link identifies build
`1a08a286b9a-6cb2deb30231f341`. The live PDF preview has 82 pages and contains
the new introduction passage reporting conditional finite-screen bounds,
the source-linked 67-issuer validation frame, no exact issuer/event matches,
and missing human outcomes. These observations establish successful pull and
compile of the manuscript update, not merely an open tab or button click.

The locally compiled PDF was visually checked on the changed regression,
bounds, and estimator pages and delivered as `main.pdf` in the main checkout.
Its SHA-256 is
`e58c877e833966218bc25fe566ce184ff9342b5e9724048150cf0c87d7dfd557`.
The generated PDF is not committed over raw or historical source material.

This receipt changes no paper text, data, labels, estimates, or sampling
authorization. The accepted implementation still reports no current-frame
DSL estimate because the new human responses are missing.
