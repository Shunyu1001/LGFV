# Integration assessment

Decision: keep the bounded implementation and manuscript update. Do not claim
completed probability validation or a realized DSL-adjusted LGFV estimate.

Three independently scoped workers produced collection materials, a numerical
engine, and conditional identification bounds. Their accepted commits and
failed attempts are retained in the experiment ledger. A fourth bounded review
pass by the bounds worker found no estimator formula error, but identified two
software risks. Actual requests now reject synthetic provenance across the
entire request and approval. Requested-analysis results no longer overwrite
the current collection status. Tests exercise both protections.

## Verified results

- The collection contains all 67 candidate units and 689 source locators.
  Every unit has at least one hash-verified source. This is source availability,
  not a judgment that all source packets suffice to classify an exit.
- All 6,298 unit/reference comparisons preserve zero exact issuer/event
  matches to the author's 94-case report. All 67 human entry records remain
  untouched. No supplied record supports a new realized human outcome.
- The engine passes 717 exhaustive artificial finite-design combinations.
  The integrated repository suite passes 141 tests. No artificial result enters
  the empirical tables or actual collection status.
- The four historical-only and contemporary-control models reproduce their
  existing results. No labels, controls, capacity crosswalks, or coefficients
  change. Adjusted estimates remain small and deletion-sensitive.
- Conditional nominal-membership bounds for the 158-issuer screen are
  38.61--100 percent, using 61 linked positives and 97 unknown outcomes.
  Without issuer/event applicability, the bounds are 0--100 percent.
- The manuscript builds to 82 pages, with the main text ending on page 27.
  New regression, bounds, and equation pages were visually inspected and are
  legible. No undefined citations or references occur. Three inherited
  appendix overfull warnings and longtable glue warnings remain.

## Attempts and limitations

The baseline five checks and integrated attempt 1 passed. The independent
review then motivated provenance and reporting corrections. Integrated
attempts 2 and 3 failed because tests still allowed the old synthetic-metadata
path, a rejection assertion passed an approval argument in the wrong position,
and a collection test read live state instead of its recorded baseline-only
snapshot. A bounded test-only follow-up was registered before attempt 4.
Attempt 4 passes all 13 commands and 141 tests. Original failures are retained.
One manual patch application failed on unmatched context and changed no files;
the corrected patch was applied before these recorded tests.

Final freeze approval has not been received. The census allocation remains a
proposal, not a realized sample or a substitute for human decisions. Reviewer
identity, actual review dates, and independent coder records are still absent
from the old author-report confirmation. No intercoder statistic is invented.
The estimate contract checks consistency, not documentary authenticity.

The raw-source and scientific-input snapshots remain protected. Existing
missing-extraction warnings concern the inherited inventory and are not hidden
by the new source-availability audit. Publication synchronization is recorded
separately after actual GitHub and Overleaf verification.
